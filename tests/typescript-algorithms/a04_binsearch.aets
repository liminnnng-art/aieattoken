!ts1
binarySearch(arr,target){let lo=0;let hi=arr.length-1;while lo<=hi{:=mid=Math.floor((lo+hi)/2);if arr[mid]===target{^mid};if arr[mid]<target{lo=mid+1}else{hi=mid-1}};^-1};main(){:=sorted=[1,3,5,7,9,11,13,15];pl(binarySearch(sorted,7));pl(binarySearch(sorted,4))}
