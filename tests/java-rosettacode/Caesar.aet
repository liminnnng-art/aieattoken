!v3;encrypt(text:string,shift:int)->string{sb:=Sb();for _,c:=rng text.toCharArray(){if (Character.isUpperCase(c)){sb.apd(rune(('A'+(c-'A'+shift)%26)))}else if (Character.isLowerCase(c)){sb.apd(rune(('a'+(c-'a'+shift)%26)))}else{sb.apd(c)}};^sb.function toString() { [native code] }()};decrypt(text:string,shift:int)->string{^encrypt(text,26-shift)};main(args:[]string){plaintext:="The quick brown fox";shift:=11;encrypted:=encrypt(plaintext,shift);decrypted:=decrypt(encrypted,shift);pl("Plain:     "+plaintext);pl("Encrypted: "+encrypted);pl("Decrypted: "+decrypted)}